import { describe, it } from "vitest";

describe("requireAdmin", () => {
  it.todo("redirects to /programs when profile.is_admin is false");
  it.todo("returns { user, profile } when profile.is_admin is true");
});
